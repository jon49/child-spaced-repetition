// Generated by LiveScript 1.2.0
var Student, r, updateCard;
Student = require('../models/student');
r = require('ramda');
updateCard = function(ctrl, cardSessionData){
  var start, cardId, deckId;
  start = cardSessionData.start, cardId = cardSessionData.cardId, deckId = cardSessionData.deckId;
  Student.store.cards = {
    lapsedTime: Date.now() - start,
    cardId: cardId,
    deckId: deckId
  };
  return ctrl.nextCard();
};
module.exports = {
  updateCard: r.curryN(3, updateCard)
};